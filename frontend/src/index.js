// Dompetku is a vanilla HTML/CSS/JS app served via /public/.
// The CRA bundle has nothing to render. Keep this file minimal so CRA still compiles.
const rootEl = document.getElementById("root");
if (rootEl) rootEl.style.display = "none";
